{
  "dependencies": {
    "localtunnel": "^2.0.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "test:security": "playwright test playwright-tests/security-admin-access.spec.ts",
    "test:security:headed": "node -e \"process.env.SLOW_MO='400'; require('child_process').execSync('npx playwright test playwright-tests/security-admin-access.spec.ts --headed', {stdio:'inherit', env:process.env})\"",
    "test:security:debug": "playwright test playwright-tests/security-admin-access.spec.ts --debug",
    "report": "playwright show-report"
  }
}
